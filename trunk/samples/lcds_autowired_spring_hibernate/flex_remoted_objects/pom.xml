<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        	<groupId>com.adobe.ac.samples.lcds_autowired_spring_hibernate</groupId>
  			<artifactId>lcds_autowired_spring_hibernate</artifactId>
  			<version>1.0-SNAPSHOT</version>
	</parent>
    <artifactId>flex_remoted_objects</artifactId>
    <packaging>swc</packaging>
    <name>${project.parent.groupId} ${project.parent.artifactId} flex_remoted_objects</name>
    <description>This one file (pom.xml) project holds the generated as3 value object.
    Those VOs are generated from the java_service_model Java VOs (through flex-annotation).
    The plugin produces the AS3 source files, and those are compiled and packaged into a swc.</description>
    <dependencies>
       <!-- flex sdk dependencies -->
  		<dependency>
            <groupId>com.adobe.flex</groupId>
            <artifactId>compiler</artifactId>
            <version>${flex.sdk.version}</version>
            <type>pom</type>
        </dependency>
        <dependency>
            <groupId>com.adobe.flex.framework</groupId>
            <artifactId>flex-framework</artifactId>
            <version>${flex.sdk.version}</version>
            <type>pom</type>
        </dependency>
        <!-- end of flex sdk dependencies -->

        <dependency>
	        <groupId>${project.groupId}</groupId>
	        <artifactId>java_core</artifactId>
	        <version>${project.version}</version>
		     <classifier>sources</classifier>
		     <type>jar</type>
		     <scope>compile</scope>
        </dependency>

		<dependency>
            <groupId>com.adobe.lcds</groupId>
            <artifactId>fds</artifactId>
            <version>${lcds.version}</version>
            <type>swc</type>
        </dependency>    
    </dependencies>
    <build>
        <sourceDirectory>target/generated-sources</sourceDirectory>
        <plugins>
            <plugin>
                <groupId>info.flex-mojos</groupId>
                <artifactId>flex-compiler-mojo</artifactId>
                <version>${flex-mojos.version}</version>
                <extensions>true</extensions>
                <configuration>
                    <locales>
                        <locale>en_US</locale>
                    </locales>
                        <includeSources>
                            <param>${project.build.sourceDirectory}</param>
                        </includeSources>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>2.0</version>
                <executions>
                    <execution>
                        <id>unpack-java-service-model</id>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <phase>generate-sources</phase>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>${project.groupId}</groupId>
                                    <artifactId>java_core</artifactId>
                                    <version>${project.version}</version>
                                    <classifier>sources</classifier>
                                    <type>jar</type>
                                    <outputDirectory>${project.build.directory}/generated-resources</outputDirectory>
                                    <includes>**/model/**/*.java</includes>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>apt-maven-plugin</artifactId>
                <version>1.0-alpha-2</version>
                <configuration>
                    <factory>de.aixcept.flex2.annotations.apt.ActionScriptAnnotationProcessorFactory</factory>
                    <force>true</force>
                    <additionalSourceRoots>
                        <param>${project.build.directory}/generated-resources</param>
                    </additionalSourceRoots>
                    <outputDirectory>${project.build.sourceDirectory}</outputDirectory>
                    <options>
                        <option>outputDir=${project.build.sourceDirectory}</option>
                        <option>debug</option>
                        <option>loglevel=3</option>
                    </options>
                    <includes>
                        <include>**/model/*</include>
                    </includes>
                </configuration>
                <executions>
                    <execution>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>process</goal>
                        </goals>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>de.aixcept</groupId>
                        <artifactId>flex-annotations</artifactId>
                        <version>1.1.0</version>
                        <exclusions>
                            <exclusion>
                                <groupId>com.sun</groupId>
                                <artifactId>tools</artifactId>
                            </exclusion>
                        </exclusions>
                    </dependency>
                    <dependency>
                        <groupId>${project.groupId}</groupId>
                        <artifactId>java_core</artifactId>
                        <version>${project.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>

    <reporting>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-project-info-reports-plugin</artifactId>
            </plugin>
            
            <plugin>
                <groupId>info.flex-mojos</groupId>
                <artifactId>asdoc-mojo</artifactId>
                <version>${flex-mojos.version}</version>
            </plugin>
        </plugins>
    </reporting>
</project>
